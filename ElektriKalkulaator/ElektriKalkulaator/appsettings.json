{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "_comment": "Portable default so a fresh clone runs without editing anything. LocalDB ships with Visual Studio. To point at your own SQL Server instead, do NOT edit this file - run: dotnet user-secrets set \"ConnectionStrings:DefaultConnection\" \"<your connection string>\". User Secrets are stored outside the project folder and are never committed.",
    "DefaultConnection": "Server=(localdb)\\MSSQLLocalDB;Database=ElektriKalkulaatorWeb;Trusted_Connection=True;TrustServerCertificate=True;MultipleActiveResultSets=True"
  }
}
