<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Identity.Stores" Version="9.0.1" />
  </ItemGroup>

  <!-- The Core project references NO other project in this solution. -->
  <!-- Core is the foundation: everything else depends on it, never the other way round. -->
  <!-- Dependency order: Core <- Data <- ApplicationServices <- Web -->

</Project>
